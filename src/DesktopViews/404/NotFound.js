import React from "react";

function NotFound() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "80vh",
      }}
    >
      <h3 style={{ color: "white", fontSize: 50 }}>Not Found</h3>
    </div>
  );
}

export default NotFound;
